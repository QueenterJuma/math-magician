import React from 'react';
import PropTypes from 'prop-types';

const Display = () => (
  <div className="display">
    <span>0</span>
  </div>
);

// const NextSolution = ({ solution }) => <p id="next-solution">{solution}</p>;
  
// NextResult.propTypes = { solution: PropTypes.string.isRequired };

// export default NextSolution;

export default Display;
